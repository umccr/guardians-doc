[project]
name = "aai-aaf-python"
version = "0.1.0"
description = "Minimal Flask backend for an AAF OIDC local demo."
requires-python = ">=3.11"
dependencies = [
    "authlib",
    "flask",
    "python-dotenv",
    "requests",
]
